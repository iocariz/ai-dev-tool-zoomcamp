for backend, use uv for dependency management. A few useful uv commands:

uv sync
uv add <package_name>
uv remove <package_name>
uv update <package_name>    
uv run <python_file.py>

regularly commit code to git